$("#messages .alert").hide();
$("#messages .ujs_alert").hide();
$("#messages .notice").hide();
$("#messages .ujs_notice").hide();
$("#buttonarea").css('visibility', 'hidden') //.hide() not used to preserve the space on the screen

$("#panel ul:visible").prepend("<%= j render 'form', :archived_val => 0 %>")
