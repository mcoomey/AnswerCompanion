ujsMessages("<%= @ujsAlert %>", "<%= @ujsNotice %>");
$("#new_textbox").closest("li").remove();  //remove the <li> element that contains the form
$("#buttonarea").css('visibility', 'hidden'); //.hide() not used to preserve the space on the screen

$("#panel ul:visible").prepend("<%= j render 'form' %>");
