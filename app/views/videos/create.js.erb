<% if @video.new_record? %>
	alert("Failed to upload video: <%= j @video.errors.full_messages.join(', ').html_safe %>");
<% else %>
  $('#videotable tr:last').after('<%= j render 'video', :video => @video, :lesson => @lesson, :subjectId => @subjectId  %>');
<% end %>