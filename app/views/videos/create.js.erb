ujsMessages("<%= @ujsAlert %>", "<%= @ujsNotice %>");
$("#ujsform").remove();
$("#buttonarea").show();

$("tbody:visible:last").append("<%= j render 'video_table_row', local_video: @video, local_course_asset_id: @course_asset.id %>");

// clear possible "No Videos found message"
$("#messages span.alert").html("");
VideoPoller.request();
