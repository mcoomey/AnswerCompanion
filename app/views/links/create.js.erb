ujsMessages("<%= @ujsAlert %>", "<%= @ujsNotice %>");
$("#ujsform").remove();
$("#buttonarea").show();

$("tbody:visible:last").append("<%= j render 'link_table_row', :local_link => @link, local_course_asset_id: @course_asset.id %>");
