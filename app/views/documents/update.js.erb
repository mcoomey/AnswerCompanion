ujsMessages("<%= @ujsAlert %>", "<%= @ujsNotice %>");
$("#ujsform").remove();
$("#buttonarea").show();

$("table tr td:nth-child(3) a").fadeTo("fast", 1).unbind('click');
$("table tr td:nth-child(4) a").fadeTo("fast", 1).unbind('click');

$("#document_id_<%= @document.id %>").replaceWith("<%= j render 'doc_table_row', local_doc: @document, local_course_asset_id: @course_asset.id %>").show();
