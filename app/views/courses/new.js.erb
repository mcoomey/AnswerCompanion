$("#messages .alert").hide();
$("#messages .ujs_alert").hide();
$("#messages .notice").hide();
$("#messages .ujs_notice").hide();
if ($.cookie('horizontal-tabs-index') == "#current-courses") {
	$("#buttonarea").hide().after("<%= j render 'new_course', :archived_val => false %>");
	}
else {
	$("#buttonarea").hide().after("<%= j render 'new_course', :archived_val => true %>");
	}