$("#messages .alert").hide();
$("#messages .ujs_alert").hide();
$("#messages .notice").hide();
$("#messages .ujs_notice").hide();
if ($.cookie('course_index_tab') == "#current-courses") {
	$("#buttonarea").hide().after("<%= j render 'new_course', :archived_val => 0 %>");
	}
else {
	$("#buttonarea").hide().after("<%= j render 'new_course', :archived_val => 1 %>");
	}