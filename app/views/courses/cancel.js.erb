$("#ujsform").remove();
$("#buttonarea").show();
$("#messages .ujs_alert").hide();
$("#messages .alert").show();
$("#messages .notice").show().html("<%= @action %> course action canceled.");
$("table tr td:nth-child(5) a").fadeTo("fast", 1).unbind('click');
$("table tr td:nth-child(6) a").fadeTo("fast", 1).unbind('click');
