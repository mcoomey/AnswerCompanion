<%= form_for(@course_asset_model_type) do |f| %>
  <% if @course_asset_model_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course_asset_model_type.errors.count, "error") %> prohibited this course_asset_model_type from being saved:</h2>

      <ul>
      <% @course_asset_model_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
