$("#ujsform").remove();
ujsMessages("<%= @ujsAlert %>", "<%= @ujsNotice %>");
$("#buttonarea").hide().after('<%= j render("edit_exercise") %>');
$("#exercise_section_title_name").autocomplete({
	  minLength: 0,
	  delay: 0,
	  source: $("#exercise_section_title_name").data("autocomplete-source")
		});