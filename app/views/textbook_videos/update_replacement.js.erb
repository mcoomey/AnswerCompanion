$("#ujsform").remove();
$("#buttonarea").show();
$("#messages .ujs_alert").hide();
$("#messages .alert").show();
$("table tr td:nth-child(2) a").fadeTo("fast", 1).unbind('click');
$("table tr td:nth-child(6) a:not(.video_hide_icon)").fadeTo("fast", 1).unbind('click');
$("table tr td:nth-child(7) a").fadeTo("fast", 1).unbind('click');
$("#messages .ujs_notice").show().html("<%= @videoNotice %>");
$("table tr#textbook_video_id_<%= @video.id %>").replaceWith("<%= j render 'tb_video_table_row', local_video:@video %>")
VideoPoller.request();
