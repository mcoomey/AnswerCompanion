ujsMessages("<%= @ujsAlert %>", "<%= @ujsNotice %>");
$("#ujsform").remove();
$("#buttonarea").show();

$("#tb_videotable table > tbody:last").append('<%= j render "tb_video_table_row", :local_video => @video  %>');

// clear possible "No Videos found message"
$("#messages span.alert").html("");
VideoPoller.request();
