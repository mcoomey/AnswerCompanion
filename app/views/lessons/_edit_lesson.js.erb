<div id="ujsform">
<%= form_for @lesson, remote:true do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title, :size => "65" %>
	
	<%= f.label :page %>
	<%= f.text_field :page, :size => "10" %>
	
  <%= f.hidden_field :course_asset_id, :value => @course_asset.id %>
  
	<%= f.submit "Update"%>
	<%= f.submit "Cancel"%>
<% end %>
</div>